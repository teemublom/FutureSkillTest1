To install the project's requirements, go to the project's root directory and type

`pip install -r requirements.txt`

And then to run the tests run

`pytest`

pytest will automatically discover and run all the tests in the project